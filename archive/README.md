# Archived prototypes

These pages preserve the earlier browser-only booking, dashboard, and online-intake experiments. They are not deployed by `vite.config.js` and are not wired to the production account, payment, or calendar data.

The files are retained for reference, not as an owner dashboard or a customer-facing workflow. Do not re-add them to the build without replacing localStorage persistence, access control, and demo messaging.
